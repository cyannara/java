 1.배열 선언
 	int[] a = new int[3];
 
 2.배열 사용
 	a[0] = 10;
 	a[1] = 10;
 	a[2] = 10;
 	a[3] = 10;  // error
 	
 3.Exception : 
 	배열 크기를 벗어난 경우 IndexOutofboundsExeptions
 	
 	
 4.배열선언과 초기화
 	int[] arr = {1,2,3,4,5};
 	
 	
 5.메모리구조 (참조변수)
 	스택                     힙
 	참조변수                
 
 
 6.배열 출력(for, for~each, length)
 	//for
 	for(int i=0; i<arr.length;i++) {
		System.out.println (arr[i]);
	}
	
	//확장 for~each
 	for (int element : arr) {
		System.out.println(element);
	}
 
 
 7.객체배열
 	Point[] p1 = {new Point(10,20), new Point(1,4), new Point(3,4)  };
 
 8.무명배열
 	Object o = new int[] {1,2,3,4,5};
 	